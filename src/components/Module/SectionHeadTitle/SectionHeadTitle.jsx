
const SectionHeadTitle = ({ title }) => {
    return (
        <div>
            <h1 className="text-center text-8xl text-slate-900">{title}</h1>
        </div>
    );
};

export default SectionHeadTitle;